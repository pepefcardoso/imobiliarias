# ---------------------------------------------------------------------------
# External dependencies
# All modules used in Phase 2 (dataclasses, typing, re, urllib, sys, os,
# unittest) are Python stdlib — no external packages required yet.
#
# Dependencies will be added as each phase is implemented:
#   Phase 3 — infrastructure:  requests, playwright
#   Phase 5 — aggregator:      (no new deps)
#   Phase 6 — API:             fastapi, uvicorn[standard]
#   Phase 8 — logging:         (stdlib logging, no extra deps)
#   Phase 9 — tests:           pytest
#   Phase 10 — UI:             (separate frontend project)
# ---------------------------------------------------------------------------

# Phase 3 — HTTP / browser clients
# requests==2.32.3
# playwright==1.44.0

# Phase 6 — API
# fastapi==0.111.0
# uvicorn[standard]==0.30.1

# Phase 9 — tests
# pytest==8.2.2