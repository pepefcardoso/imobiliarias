version: "3.8"

services:
  casasul:
    build: .
    container_name: casasul-api
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/accounts/login/')",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - traefik-public
      - internal
    env_file:
      - .env
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.casasul.rule=Host(`casasul.pepefcardoso.dev`)"
      - "traefik.http.routers.casasul.entrypoints=websecure"
      - "traefik.http.routers.casasul.tls.certresolver=letsencrypt"
      - "traefik.http.routers.casasul.middlewares=security-headers@file,casasul-ratelimit"
      - "traefik.http.services.casasul.loadbalancer.server.port=8000"
      - "traefik.http.middlewares.casasul-ratelimit.ratelimit.average=50"
      - "traefik.http.middlewares.casasul-ratelimit.ratelimit.burst=100"

  postgres:
    image: postgres:16-alpine
    container_name: casasul-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  redis:
    image: redis:7-alpine
    container_name: casasul-redis
    restart: unless-stopped
    networks:
      - internal

networks:
  traefik-public:
    external: true
  internal:

volumes:
  postgres-data:
